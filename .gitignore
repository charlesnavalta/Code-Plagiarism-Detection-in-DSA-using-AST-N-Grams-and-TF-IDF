# --- Dependencies (NEVER Upload These) ---
node_modules/
client/node_modules/
package-lock.json

# --- Python Caches (Backend Garbage) ---
__pycache__/
*.pyc
*.pyo
*.pyd

# --- Environment & Secrets ---
.env
.venv
env/
venv/
ENV/

# --- Operating System Files ---
.DS_Store
Thumbs.db

# --- System Specifics ---
# Ignore student files and guide attachments uploaded during testing, but keep directory structure
server/uploads/*
!server/uploads/.gitkeep
!server/uploads/submissions/
!server/uploads/attachments/
server/uploads/submissions/*
!server/uploads/submissions/.gitkeep
server/uploads/attachments/*
!server/uploads/attachments/.gitkeep
# Ignore the frontend build (we build it fresh in Docker)
client/build/